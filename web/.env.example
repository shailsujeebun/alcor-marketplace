# ──────────────────────────────────────────────────────
# Web (Next.js) — Environment Variables
# Copy to .env.local and fill in production values
# ──────────────────────────────────────────────────────

# ─── API URL (client-side, baked into JS at build time) ───
NEXT_PUBLIC_API_URL=https://api.your-domain.com

# ─── Translation (server-side only) ──────────────────
TRANSLATION_EXTERNAL_ENABLED=true
TRANSLATION_ALLOW_PII=false
